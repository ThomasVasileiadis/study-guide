language: bash

language: php

sudo: required

before_script:
- sudo apt-get install pandoc
- sudo apt-get install python3-venv
- python3 -m venv ./venv
- . ./venv/bin/activate
- pip install WeasyPrint

script: 
- weasyprint --help
